{
  "name": "zesty-directory",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "build-ci": "vite build --base=/zesty-directory/",
    "predeploy": "rimraf dist && vite build --base=/zesty-directory/",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "semantic-ui-css": "^2.4.1",
    "semantic-ui-react": "^2.1.3"
  },
  "devDependencies": {
    "babel-preset-nano-react-app": "^0.1.0",
    "gh-pages": "^4.0.0",
    "rimraf": "^3.0.2",
    "vite": "^2.9.13"
  },
  "babel": {
    "presets": [
      "nano-react-app"
    ]
  }
}
